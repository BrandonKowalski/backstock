services:
  backstock:
    image: ghcr.io/brandonkowalski/backstock:main
    ports:
      - "8080:8080"
    volumes:
      - backstock-data:/data
    restart: unless-stopped

volumes:
  backstock-data:
